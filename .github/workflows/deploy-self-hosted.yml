name: Deploy Self-Hosted

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5
      
      - name: Build and Deploy
        run: |
          cd /opt/voicechat
          git pull
          docker compose down
          docker compose up -d --build
